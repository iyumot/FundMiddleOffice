<UserControl
    x:Class="FMO.LawPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FMO"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <local:LawPageViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <CollectionViewSource x:Key="cata" Source="{Binding Catalog}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Chapter" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>
    <DockPanel Margin="8">
        <ListBox
            BorderThickness="0"
            ItemsSource="{Binding Source={StaticResource cata}}"
            SelectedItem="{Binding SelectedSection}">
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock FontWeight="Bold" Text="{Binding Name}" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Section}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <ListBox ItemsSource="{Binding LawList}" SelectedItem="{Binding SelectedLaw}" />


        <TextBox
            IsReadOnly="True"
            Text="{Binding LawContent}"
            TextWrapping="Wrap" />
    </DockPanel>
</UserControl>
