<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:componentmodel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:FMO.Shared">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml" />
        <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />

    </ResourceDictionary.MergedDictionaries>

    <Geometry x:Key="f.book-open-reader">M160 96a96 96 0 1 1 192 0A96 96 0 1 1 160 96zm80 152l0 264-48.4-24.2c-20.9-10.4-43.5-17-66.8-19.3l-96-9.6C12.5 457.2 0 443.5 0 427L0 224c0-17.7 14.3-32 32-32l30.3 0c63.6 0 125.6 19.6 177.7 56zm32 264l0-264c52.1-36.4 114.1-56 177.7-56l30.3 0c17.7 0 32 14.3 32 32l0 203c0 16.4-12.5 30.2-28.8 31.8l-96 9.6c-23.2 2.3-45.9 8.9-66.8 19.3L272 512z</Geometry>
    <Geometry x:Key="f.clone">M288 448L64 448l0-224 64 0 0-64-64 0c-35.3 0-64 28.7-64 64L0 448c0 35.3 28.7 64 64 64l224 0c35.3 0 64-28.7 64-64l0-64-64 0 0 64zm-64-96l224 0c35.3 0 64-28.7 64-64l0-224c0-35.3-28.7-64-64-64L224 0c-35.3 0-64 28.7-64 64l0 224c0 35.3 28.7 64 64 64z</Geometry>
    <Geometry x:Key="f.file-export">M0 64C0 28.7 28.7 0 64 0L224 0l0 128c0 17.7 14.3 32 32 32l128 0 0 128-168 0c-13.3 0-24 10.7-24 24s10.7 24 24 24l168 0 0 112c0 35.3-28.7 64-64 64L64 512c-35.3 0-64-28.7-64-64L0 64zM384 336l0-48 110.1 0-39-39c-9.4-9.4-9.4-24.6 0-33.9s24.6-9.4 33.9 0l80 80c9.4 9.4 9.4 24.6 0 33.9l-80 80c-9.4 9.4-24.6 9.4-33.9 0s-9.4-24.6 0-33.9l39-39L384 336zm0-208l-128 0L256 0 384 128z</Geometry>
    <Geometry x:Key="f.xmark">M342.6 150.6c12.5-12.5 12.5-32.8 0-45.3s-32.8-12.5-45.3 0L192 210.7 86.6 105.4c-12.5-12.5-32.8-12.5-45.3 0s-12.5 32.8 0 45.3L146.7 256 41.4 361.4c-12.5 12.5-12.5 32.8 0 45.3s32.8 12.5 45.3 0L192 301.3 297.4 406.6c12.5 12.5 32.8 12.5 45.3 0s12.5-32.8 0-45.3L237.3 256 342.6 150.6z</Geometry>
    <Geometry x:Key="f.gear">M495.9 166.6c3.2 8.7 .5 18.4-6.4 24.6l-43.3 39.4c1.1 8.3 1.7 16.8 1.7 25.4s-.6 17.1-1.7 25.4l43.3 39.4c6.9 6.2 9.6 15.9 6.4 24.6c-4.4 11.9-9.7 23.3-15.8 34.3l-4.7 8.1c-6.6 11-14 21.4-22.1 31.2c-5.9 7.2-15.7 9.6-24.5 6.8l-55.7-17.7c-13.4 10.3-28.2 18.9-44 25.4l-12.5 57.1c-2 9.1-9 16.3-18.2 17.8c-13.8 2.3-28 3.5-42.5 3.5s-28.7-1.2-42.5-3.5c-9.2-1.5-16.2-8.7-18.2-17.8l-12.5-57.1c-15.8-6.5-30.6-15.1-44-25.4L83.1 425.9c-8.8 2.8-18.6 .3-24.5-6.8c-8.1-9.8-15.5-20.2-22.1-31.2l-4.7-8.1c-6.1-11-11.4-22.4-15.8-34.3c-3.2-8.7-.5-18.4 6.4-24.6l43.3-39.4C64.6 273.1 64 264.6 64 256s.6-17.1 1.7-25.4L22.4 191.2c-6.9-6.2-9.6-15.9-6.4-24.6c4.4-11.9 9.7-23.3 15.8-34.3l4.7-8.1c6.6-11 14-21.4 22.1-31.2c5.9-7.2 15.7-9.6 24.5-6.8l55.7 17.7c13.4-10.3 28.2-18.9 44-25.4l12.5-57.1c2-9.1 9-16.3 18.2-17.8C227.3 1.2 241.5 0 256 0s28.7 1.2 42.5 3.5c9.2 1.5 16.2 8.7 18.2 17.8l12.5 57.1c15.8 6.5 30.6 15.1 44 25.4l55.7-17.7c8.8-2.8 18.6-.3 24.5 6.8c8.1 9.8 15.5 20.2 22.1 31.2l4.7 8.1c6.1 11 11.4 22.4 15.8 34.3zM256 336a80 80 0 1 0 0-160 80 80 0 1 0 0 160z</Geometry>
    <Geometry x:Key="f.gears">M308.5 135.3c7.1-6.3 9.9-16.2 6.2-25c-2.3-5.3-4.8-10.5-7.6-15.5L304 89.4c-3-5-6.3-9.9-9.8-14.6c-5.7-7.6-15.7-10.1-24.7-7.1l-28.2 9.3c-10.7-8.8-23-16-36.2-20.9L199 27.1c-1.9-9.3-9.1-16.7-18.5-17.8C173.9 8.4 167.2 8 160.4 8l-.7 0c-6.8 0-13.5 .4-20.1 1.2c-9.4 1.1-16.6 8.6-18.5 17.8L115 56.1c-13.3 5-25.5 12.1-36.2 20.9L50.5 67.8c-9-3-19-.5-24.7 7.1c-3.5 4.7-6.8 9.6-9.9 14.6l-3 5.3c-2.8 5-5.3 10.2-7.6 15.6c-3.7 8.7-.9 18.6 6.2 25l22.2 19.8C32.6 161.9 32 168.9 32 176s.6 14.1 1.7 20.9L11.5 216.7c-7.1 6.3-9.9 16.2-6.2 25c2.3 5.3 4.8 10.5 7.6 15.6l3 5.2c3 5.1 6.3 9.9 9.9 14.6c5.7 7.6 15.7 10.1 24.7 7.1l28.2-9.3c10.7 8.8 23 16 36.2 20.9l6.1 29.1c1.9 9.3 9.1 16.7 18.5 17.8c6.7 .8 13.5 1.2 20.4 1.2s13.7-.4 20.4-1.2c9.4-1.1 16.6-8.6 18.5-17.8l6.1-29.1c13.3-5 25.5-12.1 36.2-20.9l28.2 9.3c9 3 19 .5 24.7-7.1c3.5-4.7 6.8-9.5 9.8-14.6l3.1-5.4c2.8-5 5.3-10.2 7.6-15.5c3.7-8.7 .9-18.6-6.2-25l-22.2-19.8c1.1-6.8 1.7-13.8 1.7-20.9s-.6-14.1-1.7-20.9l22.2-19.8zM112 176a48 48 0 1 1 96 0 48 48 0 1 1 -96 0zM504.7 500.5c6.3 7.1 16.2 9.9 25 6.2c5.3-2.3 10.5-4.8 15.5-7.6l5.4-3.1c5-3 9.9-6.3 14.6-9.8c7.6-5.7 10.1-15.7 7.1-24.7l-9.3-28.2c8.8-10.7 16-23 20.9-36.2l29.1-6.1c9.3-1.9 16.7-9.1 17.8-18.5c.8-6.7 1.2-13.5 1.2-20.4s-.4-13.7-1.2-20.4c-1.1-9.4-8.6-16.6-17.8-18.5L583.9 307c-5-13.3-12.1-25.5-20.9-36.2l9.3-28.2c3-9 .5-19-7.1-24.7c-4.7-3.5-9.6-6.8-14.6-9.9l-5.3-3c-5-2.8-10.2-5.3-15.6-7.6c-8.7-3.7-18.6-.9-25 6.2l-19.8 22.2c-6.8-1.1-13.8-1.7-20.9-1.7s-14.1 .6-20.9 1.7l-19.8-22.2c-6.3-7.1-16.2-9.9-25-6.2c-5.3 2.3-10.5 4.8-15.6 7.6l-5.2 3c-5.1 3-9.9 6.3-14.6 9.9c-7.6 5.7-10.1 15.7-7.1 24.7l9.3 28.2c-8.8 10.7-16 23-20.9 36.2L315.1 313c-9.3 1.9-16.7 9.1-17.8 18.5c-.8 6.7-1.2 13.5-1.2 20.4s.4 13.7 1.2 20.4c1.1 9.4 8.6 16.6 17.8 18.5l29.1 6.1c5 13.3 12.1 25.5 20.9 36.2l-9.3 28.2c-3 9-.5 19 7.1 24.7c4.7 3.5 9.5 6.8 14.6 9.8l5.4 3.1c5 2.8 10.2 5.3 15.5 7.6c8.7 3.7 18.6 .9 25-6.2l19.8-22.2c6.8 1.1 13.8 1.7 20.9 1.7s14.1-.6 20.9-1.7l19.8 22.2zM464 304a48 48 0 1 1 0 96 48 48 0 1 1 0-96z</Geometry>
    <Geometry x:Key="f.angle-right">M278.6 233.4c12.5 12.5 12.5 32.8 0 45.3l-160 160c-12.5 12.5-32.8 12.5-45.3 0s-12.5-32.8 0-45.3L210.7 256 73.4 118.6c-12.5-12.5-12.5-32.8 0-45.3s32.8-12.5 45.3 0l160 160z</Geometry>
    <Geometry x:Key="f.stamp">M312 201.8c0-17.4 9.2-33.2 19.9-47C344.5 138.5 352 118.1 352 96c0-53-43-96-96-96s-96 43-96 96c0 22.1 7.5 42.5 20.1 58.8c10.7 13.8 19.9 29.6 19.9 47c0 29.9-24.3 54.2-54.2 54.2L112 256C50.1 256 0 306.1 0 368c0 20.9 13.4 38.7 32 45.3L32 464c0 26.5 21.5 48 48 48l352 0c26.5 0 48-21.5 48-48l0-50.7c18.6-6.6 32-24.4 32-45.3c0-61.9-50.1-112-112-112l-33.8 0c-29.9 0-54.2-24.3-54.2-54.2zM416 416l0 32L96 448l0-32 320 0z</Geometry>
    <GeometryGroup x:Key="f.turn-page">
        <Geometry>M12 11h5m-5-4h5m-9 8V3.6a.6.6 0 0 1 .6-.6h11.8a.6.6 0 0 1 .6.6V17a4 4 0 0 1-4 4v0</Geometry>
        <Geometry>M5 15h7.4c.331 0 .603.267.63.597C13.153 17.115 13.78 21 17 21H6a3 3 0 0 1-3-3v-1a2 2 0 0 1 2-2</Geometry>
    </GeometryGroup>

    <local:FileExistsToVisibilityConverter x:Key="FileExistsToVisibilityConverter" />

    <SolidColorBrush x:Key="LabelBrush">Gray</SolidColorBrush>

    <DataTemplate x:Key="DT.File">
        <DockPanel Background="Transparent" LastChildFill="False">
            <DockPanel.Resources>
                <Style BasedOn="{StaticResource ButtonIcon.Small}" TargetType="Button" />
            </DockPanel.Resources>
            <hc:SimplePanel Height="20">
                <local:AbbreviationText
                    x:Name="file"
                    Margin="0,2"
                    VerticalContentAlignment="Center"
                    local:MaskService.IsMask="True"
                    Text="{Binding Name}" />
                <hc:SimplePanel
                    x:Name="p"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                    Background="White"
                    Visibility="Collapsed">
                    <DockPanel Margin="5,2" LastChildFill="False">
                        <DockPanel LastChildFill="False">
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.book-open-reader}"
                                Command="{Binding ViewCommand}"
                                Foreground="Lime"
                                ToolTip="查看" />
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.clone}"
                                Command="{Binding CopyCommand}"
                                Foreground="RoyalBlue"
                                ToolTip="复制" />
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.file-export}"
                                Command="{Binding SaveAsCommand}"
                                ToolTip="另存为" />
                        </DockPanel>
                        <Button
                            Margin="-5"
                            hc:IconElement.Geometry="{StaticResource f.xmark}"
                            Command="{Binding DeleteCommand}"
                            CommandParameter="{Binding .}"
                            Foreground="Red"
                            Style="{StaticResource ButtonIcon.Small}"
                            ToolTip="删除"
                            Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                    </DockPanel>
                </hc:SimplePanel>
            </hc:SimplePanel>
            <TextBlock
                Margin="6,0,0,0"
                VerticalAlignment="Center"
                FontSize="8"
                Foreground="Red"
                Text="文件已被删除"
                Visibility="{Binding Deleted, Converter={StaticResource Boolean2VisibilityConverter}, TargetNullValue=Collapsed}" />
        </DockPanel>
        <DataTemplate.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Exists, TargetNullValue=False}" Value="True" />
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="p" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding Exists, TargetNullValue=False}" Value="False">
                <Setter TargetName="file" Property="Foreground" Value="Gray" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>


    <DataTemplate x:Key="ViewOnlyFileTemplate">
        <DockPanel Background="Transparent" LastChildFill="False">
            <DockPanel.Resources>
                <Style BasedOn="{StaticResource ButtonIcon.Small}" TargetType="Button" />
            </DockPanel.Resources>
            <hc:SimplePanel Height="20">
                <hc:SimplePanel
                    x:Name="p"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                    Background="White"
                    Visibility="Collapsed">
                    <DockPanel Margin="5,2" LastChildFill="False">
                        <DockPanel LastChildFill="False">
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.book-open-reader}"
                                Command="{Binding ViewCommand}"
                                Foreground="Lime"
                                ToolTip="查看" />
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.clone}"
                                Command="{Binding CopyCommand}"
                                Foreground="RoyalBlue"
                                ToolTip="复制" />
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.file-export}"
                                Command="{Binding SaveAsCommand}"
                                ToolTip="另存为" />
                        </DockPanel>
                    </DockPanel>
                </hc:SimplePanel>
            </hc:SimplePanel>
            <TextBlock
                Margin="6,0,0,0"
                VerticalAlignment="Center"
                FontSize="8"
                Foreground="Gray"
                Text="文件不存在"
                Visibility="{Binding Exists, Converter={StaticResource Boolean2VisibilityReConverter}, TargetNullValue=Collapsed}" />
        </DockPanel>
        <DataTemplate.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Exists, TargetNullValue=False}" Value="True" />
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=local:FileView}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="p" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <DataTemplate x:Key="FileItemTemplate">
        <DockPanel Background="Transparent" LastChildFill="False">
            <DockPanel.Resources>
                <Style BasedOn="{StaticResource ButtonIcon.Small}" TargetType="Button" />
            </DockPanel.Resources>
            <Button
                x:Name="add"
                hc:IconElement.Geometry="{StaticResource f.gear}"
                Command="{Binding ChooseCommand, FallbackValue={x:Null}}"
                Style="{StaticResource ButtonIcon.Small}"
                ToolTip="设置文件"
                Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}, Converter={StaticResource Boolean2VisibilityReConverter}}" />

            <hc:SimplePanel Height="20">
                <DockPanel x:Name="canset" MinWidth="160">
                    <TextBlock
                        VerticalAlignment="Center"
                        Foreground="RoyalBlue"
                        Text="{Binding Meta.Time, StringFormat=yyyy-MM-dd}" />

                    <local:AbbreviationText
                        x:Name="file"
                        Margin="5,0"
                        VerticalAlignment="Center"
                        local:MaskService.IsMask="True"
                        Text="{Binding Name}" />

                    <TextBlock
                        Margin="6,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="8"
                        Foreground="Red"
                        Text="文件已被删除"
                        Visibility="{Binding Deleted, Converter={StaticResource Boolean2VisibilityConverter}}" />
                </DockPanel>

                <hc:SimplePanel
                    x:Name="p"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                    Background="White"
                    Visibility="Collapsed">
                    <DockPanel Margin="5,0" LastChildFill="False">
                        <DockPanel LastChildFill="False">
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.book-open-reader}"
                                Command="{Binding ViewCommand}"
                                Foreground="Lime"
                                ToolTip="查看" />
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.clone}"
                                Command="{Binding CopyCommand}"
                                Foreground="RoyalBlue"
                                ToolTip="复制" />
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.file-export}"
                                Command="{Binding SaveAsCommand}"
                                ToolTip="另存为" />
                        </DockPanel>
                        <Button
                            hc:IconElement.Geometry="{StaticResource f.xmark}"
                            Command="{Binding DeleteCommand}"
                            CommandParameter="{Binding .}"
                            Foreground="Red"
                            Style="{StaticResource ButtonIcon.Small}"
                            ToolTip="删除"
                            Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                    </DockPanel>
                </hc:SimplePanel>
            </hc:SimplePanel>
        </DockPanel>
        <DataTemplate.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Exists, TargetNullValue=False}" Value="True" />
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="p" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding Exists, TargetNullValue=False}" Value="False">
                <Setter TargetName="file" Property="Foreground" Value="Gray" />
                <!--<Setter TargetName="add" Property="Visibility" Value="Visible" />-->
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Exists, TargetNullValue=False}" Value="False" />
                    <Condition Binding="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}}" Value="False" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="add" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Deleted, TargetNullValue=True}" Value="True" />
                    <Condition Binding="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}}" Value="False" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="add" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding Name}" Value="{x:Null}">
                <Setter TargetName="canset" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>


    <DataTemplate x:Key="FileItemTemplate.NoDate">
        <DockPanel Background="Transparent" LastChildFill="False">
            <DockPanel.Resources>
                <Style BasedOn="{StaticResource ButtonIcon.Small}" TargetType="Button" />
            </DockPanel.Resources>
            <Button
                x:Name="add"
                hc:IconElement.Geometry="{StaticResource f.gear}"
                Command="{Binding ChooseCommand, FallbackValue={x:Null}}"
                Style="{StaticResource ButtonIcon.Small}"
                ToolTip="设置文件"
                Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}, Converter={StaticResource Boolean2VisibilityReConverter}}" />

            <hc:SimplePanel Height="20">
                <DockPanel x:Name="canset" MinWidth="100">

                    <local:AbbreviationText
                        x:Name="file"
                        Margin="5,0"
                        VerticalAlignment="Center"
                        local:MaskService.IsMask="True"
                        Text="{Binding Name}" />

                    <TextBlock
                        Margin="6,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="8"
                        Foreground="Red"
                        Text="文件已被删除"
                        Visibility="{Binding Deleted, Converter={StaticResource Boolean2VisibilityConverter}}" />
                </DockPanel>

                <hc:SimplePanel
                    x:Name="p"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                    Background="White"
                    Visibility="Collapsed">
                    <DockPanel Margin="5,0" LastChildFill="False">
                        <DockPanel LastChildFill="False">
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.book-open-reader}"
                                Command="{Binding ViewCommand}"
                                Foreground="Lime"
                                ToolTip="查看" />
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.clone}"
                                Command="{Binding CopyCommand}"
                                Foreground="RoyalBlue"
                                ToolTip="复制" />
                            <Button
                                hc:IconElement.Geometry="{StaticResource f.file-export}"
                                Command="{Binding SaveAsCommand}"
                                ToolTip="另存为" />
                        </DockPanel>
                        <Button
                            hc:IconElement.Geometry="{StaticResource f.xmark}"
                            Command="{Binding DeleteCommand}"
                            CommandParameter="{Binding .}"
                            Foreground="Red"
                            Style="{StaticResource ButtonIcon.Small}"
                            ToolTip="删除"
                            Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                    </DockPanel>
                </hc:SimplePanel>
            </hc:SimplePanel>
        </DockPanel>
        <DataTemplate.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Exists, TargetNullValue=False}" Value="True" />
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="p" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>

            <DataTrigger Binding="{Binding Name}" Value="{x:Null}">
                <Setter TargetName="canset" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>


    <DataTemplate x:Key="DualFile.Flip">
        <DockPanel>
            <ToggleButton
                x:Name="btn"
                BorderThickness="0"
                Style="{StaticResource ToggleButtonFlip.Small}">
                <Viewbox Width="16" Height="16">
                    <hc:SimplePanel>
                        <Path Data="{StaticResource f.turn-page}" Fill="Black" />
                    </hc:SimplePanel>
                </Viewbox>
                <hc:StatusSwitchElement.CheckedElement>
                    <Viewbox Width="16" Height="16">
                        <Path Data="{StaticResource f.stamp}" Fill="Red" />
                    </Viewbox>
                </hc:StatusSwitchElement.CheckedElement>
            </ToggleButton>
            <ContentPresenter
                VerticalAlignment="Center"
                Content="{Binding Normal}"
                ContentTemplate="{StaticResource FileItemTemplate}"
                Visibility="{Binding ElementName=btn, Path=IsChecked, Converter={StaticResource Boolean2VisibilityReConverter}}" />
            <Border Visibility="{Binding ElementName=btn, Path=IsChecked, Converter={StaticResource Boolean2VisibilityConverter}}">
                <!--<Border.Background>
                    <LinearGradientBrush>
                        <GradientStop Offset="0" Color="#f4dab9" />
                        <GradientStop Offset="0.5" Color="#eeb7f4" />
                    </LinearGradientBrush>
                </Border.Background>-->

                <ContentPresenter
                    VerticalAlignment="Center"
                    Content="{Binding Another}"
                    ContentTemplate="{StaticResource FileItemTemplate}" />
            </Border>
        </DockPanel>
    </DataTemplate>

    <DataTemplate x:Key="MultiDualFile.Flip">
        <Border>
            <Border.Resources>
                <CollectionViewSource x:Key="src" Source="{Binding Files}">
                    <CollectionViewSource.SortDescriptions>
                        <componentmodel:SortDescription Direction="Descending" PropertyName="Normal.Meta.Time" />
                    </CollectionViewSource.SortDescriptions>
                </CollectionViewSource>
            </Border.Resources>
            <ListBox
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                ItemTemplate="{StaticResource DualFile.Flip}"
                ItemsSource="{Binding Source={StaticResource src}}"
                ScrollViewer.VerticalScrollBarVisibility="Disabled"
                Style="{StaticResource ListBoxCustom}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <hc:UniformSpacingPanel
                            IsItemsHost="True"
                            Orientation="Vertical"
                            VerticalSpacing="2" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </Border>
    </DataTemplate>


    <DataTemplate x:Key="DualFile.Wrap">
        <hc:UniformSpacingPanel ChildWrapping="Wrap" Spacing="5">

            <ContentPresenter
                VerticalAlignment="Center"
                Content="{Binding Normal}"
                ContentTemplate="{StaticResource FileItemTemplate}" />

            <Viewbox
                x:Name="stp"
                Width="16"
                Height="16">
                <Path Data="{StaticResource f.stamp}" Fill="Red" />
            </Viewbox>

            <Border>
                <ContentPresenter
                    VerticalAlignment="Center"
                    Content="{Binding Another}"
                    ContentTemplate="{StaticResource FileItemTemplate}" />
            </Border>
        </hc:UniformSpacingPanel>
        <DataTemplate.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Another.Exists}" Value="False" />
                    <Condition Binding="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="stp" Property="Visibility" Value="Collapsed" />
            </MultiDataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>


    <DataTemplate x:Key="DualFile.Wrap.NoDate">
        <hc:UniformSpacingPanel ChildWrapping="Wrap" Spacing="5">
            <ContentPresenter
                VerticalAlignment="Center"
                AllowDrop="{Binding IsReadOnly, Converter={StaticResource Boolean2BooleanReConverter}, RelativeSource={RelativeSource AncestorType=local:FileView}}"
                Content="{Binding Normal}"
                ContentTemplate="{StaticResource FileItemTemplate.NoDate}" />

            <Viewbox
                x:Name="stp"
                Width="16"
                Height="16"
                Margin="5,0,0,0">
                <Path Data="{StaticResource f.stamp}" Fill="Red" />
            </Viewbox>

            <Border Margin="5,0,0,0">
                <ContentPresenter
                    VerticalAlignment="Center"
                    AllowDrop="{Binding IsReadOnly, Converter={StaticResource Boolean2BooleanReConverter}, RelativeSource={RelativeSource AncestorType=local:FileView}}"
                    Content="{Binding Another}"
                    ContentTemplate="{StaticResource FileItemTemplate.NoDate}" />
            </Border>
        </hc:UniformSpacingPanel>
        <DataTemplate.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Another.Exists}" Value="False" />
                    <Condition Binding="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="stp" Property="Visibility" Value="Collapsed" />
            </MultiDataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <DataTemplate x:Key="MultiDualFile.Wrap">
        <hc:SimplePanel>
            <hc:SimplePanel.Resources>
                <CollectionViewSource x:Key="src" Source="{Binding Files}">
                    <CollectionViewSource.SortDescriptions>
                        <componentmodel:SortDescription Direction="Descending" PropertyName="Normal.Time" />
                    </CollectionViewSource.SortDescriptions>
                </CollectionViewSource>
            </hc:SimplePanel.Resources>
            <ListBox
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                ItemTemplate="{StaticResource DualFile.Wrap}"
                ItemsSource="{Binding Source={StaticResource src}}"
                ScrollViewer.VerticalScrollBarVisibility="Disabled"
                Style="{StaticResource ListBoxCustom}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <hc:UniformSpacingPanel
                            IsItemsHost="True"
                            Orientation="Vertical"
                            VerticalSpacing="2" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </hc:SimplePanel>
    </DataTemplate>

    <!--  Style for MultipleFileView  -->
    <Style x:Key="MultiDualFileStyle" TargetType="local:FileView">
        <Setter Property="AllowDrop" Value="{Binding IsReadOnly, RelativeSource={RelativeSource Self}, Converter={StaticResource Boolean2BooleanReConverter}}" />

        <Setter Property="ContentTemplate" Value="{StaticResource MultiDualFile.Wrap}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:FileView">
                    <Border
                        MaxHeight="20"
                        Margin="{TemplateBinding Margin}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <i:Interaction.Behaviors>
                            <local:DelayMouseOverExpandBehavior TargetMaxHeight="200" />
                        </i:Interaction.Behaviors>
                        <DockPanel Background="Transparent">
                            <ContentPresenter
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}" />

                            <hc:SimplePanel>
                                <Viewbox
                                    x:Name="arrow"
                                    Width="14"
                                    Height="14"
                                    Margin="2,0,8,0"
                                    VerticalAlignment="Center">
                                    <Path
                                        Data="{StaticResource f.angle-right}"
                                        Fill="Black"
                                        Stroke="Black" />
                                </Viewbox>
                                <Button
                                    x:Name="add"
                                    hc:IconElement.Geometry="{StaticResource f.gear}"
                                    Command="{Binding Content.AddFileCommand, RelativeSource={RelativeSource TemplatedParent}, FallbackValue={x:Null}}"
                                    CommandParameter="{Binding ., RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{StaticResource ButtonIcon.Small}"
                                    ToolTip="设置文件"
                                    Visibility="Hidden" />
                            </hc:SimplePanel>

                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsReadOnly" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="add" Property="Visibility" Value="Visible" />
                            <Setter TargetName="arrow" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <DataTemplate x:Key="MultiFile">
        <hc:SimplePanel>
            <ListBox
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                ItemTemplate="{StaticResource DT.File}"
                ItemsSource="{Binding Files}"
                ScrollViewer.VerticalScrollBarVisibility="Disabled"
                Style="{StaticResource ListBoxCustom}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <hc:UniformSpacingPanel
                            IsItemsHost="True"
                            Orientation="Vertical"
                            VerticalSpacing="2" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </hc:SimplePanel>
    </DataTemplate>

    <DataTemplate x:Key="MultiFile.Date">
        <hc:SimplePanel>
            <hc:SimplePanel.Resources>
                <CollectionViewSource x:Key="src" Source="{Binding Files}">
                    <CollectionViewSource.SortDescriptions>
                        <componentmodel:SortDescription Direction="Descending" PropertyName="Time" />
                    </CollectionViewSource.SortDescriptions>
                </CollectionViewSource>
            </hc:SimplePanel.Resources>
            <ListBox
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                ItemTemplate="{StaticResource DT.File}"
                ItemsSource="{Binding Source={StaticResource src}}"
                ScrollViewer.VerticalScrollBarVisibility="Disabled"
                Style="{StaticResource ListBoxCustom}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <hc:UniformSpacingPanel
                            IsItemsHost="True"
                            Orientation="Vertical"
                            VerticalSpacing="2" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </hc:SimplePanel>
    </DataTemplate>

    <DataTemplate x:Key="DefaultHeader">
        <TextBlock Text="{Binding}" TextElement.Foreground="{StaticResource LabelBrush}" />
    </DataTemplate>


    <Style x:Key="MultiFileStyle" TargetType="local:FileView">
        <Setter Property="AllowDrop" Value="{Binding IsReadOnly, RelativeSource={RelativeSource Self}, Converter={StaticResource Boolean2BooleanReConverter}}" />
        <Setter Property="ContentTemplate" Value="{StaticResource MultiFile}" />
        <Setter Property="Header" Value="{Binding Content.Label, RelativeSource={RelativeSource Self}, FallbackValue=文件}" />
        <Setter Property="HeaderTemplate" Value="{StaticResource DefaultHeader}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:FileView">
                    <Border
                        MaxHeight="20"
                        Margin="{TemplateBinding Margin}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <i:Interaction.Behaviors>
                            <local:DelayMouseOverExpandBehavior TargetMaxHeight="200" />
                        </i:Interaction.Behaviors>
                        <DockPanel Background="Transparent">
                            <ContentPresenter
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}" />

                            <hc:SimplePanel>
                                <Viewbox
                                    x:Name="arrow"
                                    Width="14"
                                    Height="14"
                                    Margin="2,0,8,0"
                                    VerticalAlignment="Center">
                                    <Path
                                        Data="{StaticResource f.angle-right}"
                                        Fill="Black"
                                        Stroke="Black" />
                                </Viewbox>
                                <Button
                                    x:Name="add"
                                    hc:IconElement.Geometry="{StaticResource f.gear}"
                                    Command="{Binding Content.AddFileCommand, RelativeSource={RelativeSource TemplatedParent}, FallbackValue={x:Null}}"
                                    CommandParameter="{Binding ., RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{StaticResource ButtonIcon.Small}"
                                    ToolTip="设置文件"
                                    Visibility="Hidden" />
                            </hc:SimplePanel>

                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsReadOnly" Value="False" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="add" Property="Visibility" Value="Visible" />
                            <Setter TargetName="arrow" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>

                        <!--<MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=local:FileView}}" Value="False"/>
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="add" Property="Visibility" Value="Visible" />
                            <Setter TargetName="arrow" Property="Visibility" Value="Collapsed" />
                        </MultiDataTrigger>-->

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>




    <Style x:Key="SimpleFileStyle" TargetType="local:FileView">
        <Setter Property="AllowDrop" Value="{Binding IsReadOnly, RelativeSource={RelativeSource Self}, Converter={StaticResource Boolean2BooleanReConverter}}" />
        <Setter Property="ContentTemplate" Value="{StaticResource DT.File}" />
        <Setter Property="HeaderTemplate" Value="{StaticResource DefaultHeader}" />
        <!--<Setter Property="Header" Value="{Binding Content.Label, RelativeSource={RelativeSource Self}, FallbackValue=文件}" />-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:FileView">
                    <Border
                        MaxHeight="20"
                        Margin="{TemplateBinding Margin}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <i:Interaction.Behaviors>
                            <local:DelayMouseOverExpandBehavior TargetMaxHeight="200" />
                        </i:Interaction.Behaviors>
                        <DockPanel Background="Transparent">
                            <ContentPresenter
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}" />

                            <hc:SimplePanel>
                                <Viewbox
                                    x:Name="arrow"
                                    Width="14"
                                    Height="14"
                                    Margin="2,0,8,0"
                                    VerticalAlignment="Center">
                                    <Path
                                        Data="{StaticResource f.angle-right}"
                                        Fill="Black"
                                        Stroke="Black" />
                                </Viewbox>
                                <Button
                                    x:Name="add"
                                    hc:IconElement.Geometry="{StaticResource f.gear}"
                                    Command="{Binding Content.ChooseCommand, RelativeSource={RelativeSource TemplatedParent}, FallbackValue={x:Null}}"
                                    CommandParameter="{Binding ., RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{StaticResource ButtonIcon.Small}"
                                    ToolTip="设置文件"
                                    Visibility="Hidden" />
                            </hc:SimplePanel>

                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsReadOnly" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="add" Property="Visibility" Value="Visible" />
                            <Setter TargetName="arrow" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>
                        <!--<DataTrigger Binding="{Binding Exists}" Value="True">
                            <Setter TargetName="add" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="arrow" Property="Visibility" Value="Visible" />
                        </DataTrigger>-->
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="DualFileStyle" TargetType="local:FileView">
        <!--<Setter Property="AllowDrop" Value="{Binding IsReadOnly, RelativeSource={RelativeSource Self}, Converter={StaticResource Boolean2BooleanReConverter}}" />-->
        <Setter Property="ContentTemplate" Value="{StaticResource DualFile.Wrap.NoDate}" />
        <Setter Property="HeaderTemplate" Value="{StaticResource DefaultHeader}" />
        <Setter Property="Header" Value="{Binding Content.Label, RelativeSource={RelativeSource Self}, FallbackValue=文件}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:FileView">
                    <Border
                        Margin="{TemplateBinding Margin}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <DockPanel Background="Transparent">
                            <ContentPresenter
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}" />

                            <hc:SimplePanel>
                                <Viewbox
                                    x:Name="arrow"
                                    Width="14"
                                    Height="14"
                                    Margin="2,0,8,0"
                                    VerticalAlignment="Center">
                                    <Path
                                        Data="{StaticResource f.angle-right}"
                                        Fill="Black"
                                        Stroke="Black" />
                                </Viewbox>

                            </hc:SimplePanel>

                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="SimpleViewOnlyFileStyle" TargetType="local:FileView">
        <Setter Property="ContentTemplate" Value="{StaticResource ViewOnlyFileTemplate}" />
        <Setter Property="HeaderTemplate" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:FileView">
                    <Border
                        MaxHeight="20"
                        Margin="{TemplateBinding Margin}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <DockPanel Background="Transparent" LastChildFill="False">
                            <Border x:Name="b1" Background="#e1e1e1">
                                <ContentPresenter
                                    Margin="6,0"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding Header}"
                                    ContentTemplate="{TemplateBinding HeaderTemplate}" />
                            </Border>

                            <hc:SimplePanel>
                                <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                                <Border BorderBrush="#e1e1e1" BorderThickness="0,0,0,1" />
                            </hc:SimplePanel>
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Content.Exists, FallbackValue=False}" Value="False">
                            <Setter TargetName="b1" Property="Background" Value="Red" />
                            <Setter TargetName="b1" Property="TextElement.Foreground" Value="White" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style BasedOn="{StaticResource SimpleFileStyle}" TargetType="local:FileView" />

</ResourceDictionary>