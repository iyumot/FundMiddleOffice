<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:FMO.Shared">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml" />
        <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  DataTemplate for Multiple File Items  -->
    <DataTemplate x:Key="DT.MultiFile">
        <DockPanel Background="Transparent" LastChildFill="False">
            <hc:SimplePanel>
                <local:AbbreviationText
                    x:Name="file"
                    VerticalContentAlignment="Center"
                    Text="{Binding File.Name}" />
                <hc:SimplePanel
                    x:Name="p"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                    Background="White"
                    Visibility="Collapsed">
                    <hc:SimplePanel.Resources>
                        <Style BasedOn="{StaticResource ButtonIcon.Small}" TargetType="Button">
                            <Setter Property="Margin" Value="2,-5" />
                        </Style>
                    </hc:SimplePanel.Resources>
                    <DockPanel Margin="5,2" LastChildFill="False">
                        <DockPanel LastChildFill="False">
                            <Button
                                hc:IconElement.Geometry="{DynamicResource f.book-open-reader}"
                                Command="{Binding ViewCommand}"
                                Foreground="Lime"
                                ToolTip="查看" />
                            <Button
                                hc:IconElement.Geometry="{DynamicResource f.clone}"
                                Command="{Binding CopyCommand}"
                                Foreground="RoyalBlue"
                                ToolTip="复制" />
                            <Button
                                hc:IconElement.Geometry="{DynamicResource f.file-export}"
                                Command="{Binding SaveAsCommand}"
                                ToolTip="另存为" />
                        </DockPanel>
                        <Button
                            Margin="-5"
                            hc:IconElement.Geometry="{DynamicResource f.xmark}"
                            Command="{Binding DataContext.DeleteFileCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding .}"
                            Foreground="Red"
                            Style="{DynamicResource ButtonIcon.Small}"
                            ToolTip="删除"
                            Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=ListBox}, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                    </DockPanel>
                </hc:SimplePanel>
            </hc:SimplePanel>
            <TextBlock
                Margin="6,0,0,0"
                VerticalAlignment="Center"
                FontSize="8"
                Foreground="Red"
                Text="文件已被删除"
                Visibility="{Binding Deleted, Converter={StaticResource Boolean2VisibilityConverter}}" />
        </DockPanel>
        <DataTemplate.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Exists, TargetNullValue=False}" Value="True" />
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="p" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding Deleted, TargetNullValue=False}" Value="True">
                <Setter TargetName="file" Property="Foreground" Value="Gray" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <!--  DataTemplate for Single File View Items  -->
    <DataTemplate x:Key="DT.File">
        <DockPanel Background="Transparent" LastChildFill="False">
            <hc:SimplePanel>
                <local:AbbreviationText
                    x:Name="file"
                    Margin="0,2"
                    VerticalContentAlignment="Center"
                    local:MaskService.IsMask="True"
                    Text="{Binding Name}" />
                <hc:SimplePanel
                    x:Name="p"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                    Background="White"
                    Visibility="Collapsed">
                    <hc:SimplePanel.Resources>
                        <Style BasedOn="{StaticResource ButtonIcon.Small}" TargetType="Button">
                            <Setter Property="Margin" Value="2,-5" />
                        </Style>
                    </hc:SimplePanel.Resources>
                    <DockPanel Margin="5,2" LastChildFill="False">
                        <DockPanel LastChildFill="False">
                            <Button
                                hc:IconElement.Geometry="{DynamicResource f.book-open-reader}"
                                Command="{Binding RelativeSource={RelativeSource AncestorType=local:MultipleFileView}, Path=Binding.ViewCommand}"
                                CommandParameter="{Binding .}"
                                Foreground="Lime"
                                ToolTip="查看" />
                            <Button
                                hc:IconElement.Geometry="{DynamicResource f.clone}"
                                Command="{Binding RelativeSource={RelativeSource AncestorType=local:MultipleFileView}, Path=Binding.CopyCommand}"
                                CommandParameter="{Binding .}"
                                Foreground="RoyalBlue"
                                ToolTip="复制" />
                            <Button
                                hc:IconElement.Geometry="{DynamicResource f.file-export}"
                                Command="{Binding RelativeSource={RelativeSource AncestorType=local:MultipleFileView}, Path=Binding.SaveAsCommand}"
                                CommandParameter="{Binding .}"
                                ToolTip="另存为" />
                        </DockPanel>
                        <Button
                            Margin="-5"
                            hc:IconElement.Geometry="{DynamicResource f.xmark}"
                            Command="{Binding Binding.DeleteFileCommand, RelativeSource={RelativeSource AncestorType=local:MultipleFileView}}"
                            CommandParameter="{Binding .}"
                            Foreground="Red"
                            Style="{DynamicResource ButtonIcon.Small}"
                            ToolTip="删除"
                            Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=ListBox}, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                    </DockPanel>
                </hc:SimplePanel>
            </hc:SimplePanel>
            <TextBlock
                Margin="6,0,0,0"
                VerticalAlignment="Center"
                FontSize="8"
                Foreground="Red"
                Text="文件已被删除"
                Visibility="{Binding Exists, Converter={StaticResource Boolean2VisibilityReConverter}}" />
        </DockPanel>
        <DataTemplate.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Exists, TargetNullValue=False}" Value="True" />
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter TargetName="p" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding Exists, TargetNullValue=False}" Value="False">
                <Setter TargetName="file" Property="Foreground" Value="Gray" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <!--  Style for FileControl  -->
    <Style TargetType="{x:Type local:FileControl}">
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="AllowDrop" Value="{Binding IsReadOnly, Converter={StaticResource Boolean2BooleanReConverter}}" />
        <Setter Property="MinHeight" Value="20" />
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <DockPanel LastChildFill="False">
                        <TextBlock
                            Name="lb"
                            VerticalAlignment="Center"
                            Text="{Binding Binding.Label, RelativeSource={RelativeSource AncestorType=local:FileControl}}" />
                        <Viewbox
                            Width="14"
                            Height="14"
                            Margin="2,0,8,0"
                            VerticalAlignment="Center">
                            <Path
                                Data="{DynamicResource f.angle-right}"
                                Fill="Black"
                                Stroke="Black" />
                        </Viewbox>
                    </DockPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FileControl}">
                    <Border HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                        <DockPanel Background="Transparent" LastChildFill="False">
                            <ContentPresenter VerticalAlignment="Center" ContentSource="Header" />
                            <hc:SimplePanel Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityReConverter}}">
                                <Button
                                    hc:IconElement.Geometry="{DynamicResource f.gear}"
                                    Command="{Binding ChooseFileCommand}"
                                    CommandParameter="{Binding Binding, RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{DynamicResource ButtonIcon.Small}"
                                    ToolTip="设置文件"
                                    Visibility="{Binding Binding.Exists, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                            </hc:SimplePanel>
                            <hc:SimplePanel>
                                <local:AbbreviationText
                                    x:Name="file"
                                    VerticalContentAlignment="Center"
                                    local:MaskService.IsMask="True"
                                    Text="{Binding Binding.File.Name, RelativeSource={RelativeSource TemplatedParent}}" />
                                <hc:SimplePanel
                                    x:Name="p"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Stretch"
                                    Background="White"
                                    Visibility="Collapsed">
                                    <hc:SimplePanel.Resources>
                                        <Style BasedOn="{StaticResource ButtonIcon.Small}" TargetType="Button">
                                            <Setter Property="Margin" Value="2,-5" />
                                        </Style>
                                    </hc:SimplePanel.Resources>
                                    <DockPanel Margin="5,2" LastChildFill="False">
                                        <DockPanel DataContext="{Binding Binding, RelativeSource={RelativeSource TemplatedParent}}" LastChildFill="False">
                                            <Button
                                                hc:IconElement.Geometry="{DynamicResource f.book-open-reader}"
                                                Command="{Binding ViewCommand}"
                                                Foreground="Lime"
                                                ToolTip="查看" />
                                            <Button
                                                hc:IconElement.Geometry="{DynamicResource f.clone}"
                                                Command="{Binding CopyCommand}"
                                                Foreground="RoyalBlue"
                                                ToolTip="复制" />
                                            <Button
                                                hc:IconElement.Geometry="{DynamicResource f.file-export}"
                                                Command="{Binding SaveAsCommand}"
                                                ToolTip="另存为" />
                                        </DockPanel>
                                        <Button
                                            hc:IconElement.Geometry="{DynamicResource f.xmark}"
                                            Command="{Binding ClearCommand}"
                                            CommandParameter="{Binding Binding, RelativeSource={RelativeSource TemplatedParent}}"
                                            Foreground="Red"
                                            ToolTip="删除"
                                            Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                                    </DockPanel>
                                </hc:SimplePanel>
                            </hc:SimplePanel>
                            <TextBlock
                                Margin="6,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="8"
                                Foreground="Red"
                                Text="文件已被删除"
                                Visibility="{Binding Binding.Deleted, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Binding.Exists, TargetNullValue=False, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="p" Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                        <DataTrigger Binding="{Binding Binding.Deleted, TargetNullValue=False, RelativeSource={RelativeSource Self}}" Value="True">
                            <Setter TargetName="file" Property="Foreground" Value="Gray" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style for MultiFileControl  -->
    <Style TargetType="local:MultiFileControl">
        <Setter Property="MinHeight" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:MultiFileControl">
                    <Border>
                        <DockPanel Background="Transparent">
                            <TextBlock
                                x:Name="lb"
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource LabelBrush}"
                                Text="{Binding Binding.Label, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Grid>
                                <Viewbox
                                    x:Name="arrow"
                                    Width="14"
                                    Height="14"
                                    Margin="2,0,8,0"
                                    VerticalAlignment="Center">
                                    <Path
                                        Data="{DynamicResource f.angle-right}"
                                        Fill="Black"
                                        Stroke="Black" />
                                </Viewbox>
                                <Button
                                    x:Name="add"
                                    hc:IconElement.Geometry="{DynamicResource f.gear}"
                                    Command="{Binding AddFileCommand}"
                                    CommandParameter="{Binding Binding, RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{DynamicResource ButtonIcon.Small}"
                                    ToolTip="设置文件"
                                    Visibility="Collapsed" />
                            </Grid>
                            <ListBox
                                Background="Transparent"
                                BorderThickness="0"
                                ItemTemplate="{StaticResource DT.MultiFile}"
                                ItemsSource="{Binding Binding.Files, RelativeSource={RelativeSource TemplatedParent}}"
                                Style="{DynamicResource ListBoxCustom}" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsReadOnly" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="add" Property="Visibility" Value="Visible" />
                            <Setter TargetName="arrow" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style for MultipleFileView  -->
    <Style TargetType="local:MultipleFileView">
        <Setter Property="AllowDrop" Value="{Binding IsReadOnly, Converter={StaticResource Boolean2BooleanReConverter}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:MultipleFileView">
                    <Border>
                        <DockPanel Background="Transparent">
                            <TextBlock
                                x:Name="lb"
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource LabelBrush}"
                                Text="{Binding Binding.Label, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Grid>
                                <Viewbox
                                    x:Name="arrow"
                                    Width="14"
                                    Height="14"
                                    Margin="2,0,8,0"
                                    VerticalAlignment="Center">
                                    <Path
                                        Data="{DynamicResource f.angle-right}"
                                        Fill="Black"
                                        Stroke="Black" />
                                </Viewbox>
                                <Button
                                    x:Name="add"
                                    hc:IconElement.Geometry="{DynamicResource f.gear}"
                                    Command="{Binding Binding.AddFileCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                    CommandParameter="{Binding ., RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{DynamicResource ButtonIcon.Small}"
                                    ToolTip="设置文件"
                                    Visibility="Hidden" />
                            </Grid>
                            <ListBox
                                Background="Transparent"
                                BorderThickness="0"
                                ItemTemplate="{StaticResource DT.File}"
                                ItemsSource="{Binding Binding.Files, RelativeSource={RelativeSource TemplatedParent}}"
                                Style="{DynamicResource ListBoxCustom}" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsReadOnly" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="add" Property="Visibility" Value="Visible" />
                            <Setter TargetName="arrow" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style for SingleFileView  -->
    <Style TargetType="local:SingleFileView">
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="AllowDrop" Value="{Binding IsReadOnly, RelativeSource={RelativeSource Self}, Converter={StaticResource Boolean2BooleanReConverter}}" />
        <Setter Property="MinHeight" Value="20" />
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <DockPanel LastChildFill="False">
                        <Viewbox
                            Width="8"
                            Margin="2,0"
                            Visibility="{Binding Binding.IsRequired, RelativeSource={RelativeSource AncestorType=local:SingleFileView}, Converter={StaticResource Boolean2VisibilityConverter}}">
                            <Path Data="{StaticResource StarGeometry}" Fill="Red" />
                        </Viewbox>
                        <TextBlock
                            Name="lb"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource LabelBrush}"
                            Text="{Binding Binding.Label, RelativeSource={RelativeSource AncestorType=local:SingleFileView}}" />
                        <Viewbox
                            Width="14"
                            Height="14"
                            Margin="2,0,8,0"
                            VerticalAlignment="Center">
                            <Path
                                Data="{DynamicResource f.angle-right}"
                                Fill="Black"
                                Stroke="Black" />
                        </Viewbox>
                    </DockPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SingleFileView}">
                    <Border HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                        <DockPanel Background="Transparent" LastChildFill="False">
                            <ContentPresenter VerticalAlignment="Center" ContentSource="Header" />
                            <hc:SimplePanel Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityReConverter}}">
                                <Button
                                    hc:IconElement.Geometry="{DynamicResource f.gear}"
                                    Command="{Binding Binding.SetFileCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{DynamicResource ButtonIcon.Small}"
                                    ToolTip="设置文件"
                                    Visibility="{Binding Binding.Exists, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                            </hc:SimplePanel>
                            <hc:SimplePanel>
                                <local:AbbreviationText
                                    x:Name="file"
                                    VerticalContentAlignment="Center"
                                    local:MaskService.IsMask="True"
                                    Text="{Binding Binding.File.Name, RelativeSource={RelativeSource TemplatedParent}}" />
                                <hc:SimplePanel
                                    x:Name="p"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Stretch"
                                    Background="White"
                                    Visibility="Collapsed">
                                    <hc:SimplePanel.Resources>
                                        <Style BasedOn="{StaticResource ButtonIcon.Small}" TargetType="Button">
                                            <Setter Property="Margin" Value="2,-5" />
                                        </Style>
                                    </hc:SimplePanel.Resources>
                                    <DockPanel Margin="5,2" LastChildFill="False">
                                        <DockPanel DataContext="{Binding Binding, RelativeSource={RelativeSource TemplatedParent}}" LastChildFill="False">
                                            <Button
                                                hc:IconElement.Geometry="{DynamicResource f.book-open-reader}"
                                                Command="{Binding ViewCommand}"
                                                Foreground="Lime"
                                                ToolTip="查看" />
                                            <Button
                                                hc:IconElement.Geometry="{DynamicResource f.clone}"
                                                Command="{Binding CopyCommand}"
                                                Foreground="RoyalBlue"
                                                ToolTip="复制" />
                                            <Button
                                                hc:IconElement.Geometry="{DynamicResource f.file-export}"
                                                Command="{Binding SaveAsCommand}"
                                                ToolTip="另存为" />
                                        </DockPanel>
                                        <Button
                                            hc:IconElement.Geometry="{DynamicResource f.xmark}"
                                            Command="{Binding Binding.DeleteFileCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                            Foreground="Red"
                                            ToolTip="删除"
                                            Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                                    </DockPanel>
                                </hc:SimplePanel>
                            </hc:SimplePanel>
                            <TextBlock
                                Margin="6,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="8"
                                Foreground="Red"
                                Text="文件已被删除"
                                Visibility="{Binding Binding.Deleted, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Binding.Exists, TargetNullValue=False, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="p" Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                        <DataTrigger Binding="{Binding Binding.Deleted, TargetNullValue=False, RelativeSource={RelativeSource Self}}" Value="True">
                            <Setter TargetName="file" Property="Foreground" Value="Gray" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>